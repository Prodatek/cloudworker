# No resources exist yet in Phase 1, so there is nothing to output.
# Outputs (VPC id, S3 bucket names, launch template id, ...) are added in Phase 3+
# as the corresponding modules/resources are introduced.
